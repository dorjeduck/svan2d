"""Skia versions of the scene transitions, one module per transition.

Each mirrors its transition's composite() on a skia.Canvas; the transitions
name them with @skia_transition (svan2d.transition.scene.registry).
"""
